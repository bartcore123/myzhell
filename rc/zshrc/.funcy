alias '#'='\#'
./ () {
	[[ -n $@ ]] && zmv -n $@ || pwd
}
#umacro () {
#	macros=(${macros//${(j:|:)~@}}) 
#}
umacro () {
	macros=(${macros//${(j:|:)~@}}) 
	unalias $@
}
nz () {
	{
		sed -r '/'"$*"'/{F;=;p};d' ~Z/*
	} | $
	nano "$r[1]:$r[2]"
}
